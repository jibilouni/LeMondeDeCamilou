---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

import { getCollection } from 'astro:content';

const blogPosts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// Compter les articles par catégorie
const categories = {
    'Tous': blogPosts.length,
    'Yoga': blogPosts.filter(p => p.data.category === 'Yoga').length,
    'Prothésie Ongulaire': blogPosts.filter(p => p.data.category === 'Prothésie Ongulaire').length,
    'Esthétique': blogPosts.filter(p => p.data.category === 'Esthétique').length,
    'Événements': blogPosts.filter(p => p.data.category === 'Événements').length,
};

// Schema.org pour SEO
const schemaBlog = {
    "@context": "https://schema.org",
    "@type": "Blog",
    "name": "Blog Le Monde de Camilou",
    "description": "Conseils bien-être, yoga, beauté et événements à Lorient",
    "url": "https://lemondedecamilou.fr/blog",
    "publisher": {
        "@type": "Organization",
        "name": "Le Monde de Camilou",
        "logo": {
            "@type": "ImageObject",
            "url": "https://lemondedecamilou.fr/logo.png"
        }
    }
};
---

<Layout 
    title="Blog Le Monde de Camilou | Conseils Yoga, Beauté & Événements Lorient" 
    description="✨ Découvrez nos conseils d'experts : yoga bien-être, nail art tendances, soins esthétiques naturels, organisation événements. Articles pratiques par nos professionnelles à Lorient."
>
    <!-- Schema.org -->
    <script type="application/ld+json" set:html={JSON.stringify(schemaBlog)} />
    
    <Header />
  
    <main>
        <!-- Hero Section - ULTRA AMÉLIORÉ -->
        <section class="relative bg-gradient-to-br from-peche-clair/30 via-white to-rose-poudre/20 py-20 md:py-28 px-4 overflow-hidden">
            <!-- Formes décoratives -->
            <div class="absolute top-20 right-10 w-72 h-72 bg-rose-300/20 rounded-full blur-3xl animate-pulse"></div>
            <div class="absolute bottom-20 left-10 w-96 h-96 bg-peche-light/30 rounded-full blur-3xl animate-pulse delay-1000"></div>
            
            <div class="container mx-auto text-center relative z-10">
                <!-- Badge -->
                <div class="inline-flex items-center gap-2 bg-white/90 backdrop-blur-sm px-5 py-2 rounded-full mb-6 shadow-lg">
                    <svg class="w-4 h-4 text-rose-500" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M9 4.804A7.968 7.968 0 005.5 4c-1.255 0-2.443.29-3.5.804v10A7.969 7.969 0 015.5 14c1.669 0 3.218.51 4.5 1.385A7.962 7.962 0 0114.5 14c1.255 0 2.443.29 3.5.804v-10A7.968 7.968 0 0014.5 4c-1.255 0-2.443.29-3.5.804V12a1 1 0 11-2 0V4.804z"/>
                    </svg>
                    <span class="text-sm font-semibold text-gray-700">📚 {blogPosts.length} articles · Conseils d'expertes</span>
                </div>

                <h1 class="text-5xl md:text-6xl lg:text-7xl font-display font-bold mb-6 leading-tight">
                    <span class="text-gris-texte">Notre </span><span class="bg-gradient-to-r from-rose-poudre via-rose-400 to-corail-warmth bg-clip-text text-transparent">Blog</span>
                </h1>
                
                <p class="text-xl md:text-2xl text-gray-600 max-w-3xl mx-auto mb-8 leading-relaxed">
                    <strong>Conseils pratiques, astuces bien-être</strong> et actualités autour du <strong>yoga, beauté et événements</strong> à Lorient. Par nos professionnelles passionnées ✨
                </p>

                <!-- Barre de recherche (optionnelle) -->
                <div class="max-w-2xl mx-auto">
                    <div class="relative">
                        <input 
                            type="text" 
                            placeholder="🔍 Rechercher un article (yoga, nail art, événement...)"
                            class="w-full px-6 py-4 rounded-full border-2 border-rose-200 focus:border-rose-400 focus:outline-none shadow-lg text-gray-700"
                        />
                        <button class="absolute right-2 top-2 bg-gradient-to-r from-rose-400 to-coral-500 text-white px-6 py-2 rounded-full font-semibold hover:scale-105 transition-all">
                            Rechercher
                        </button>
                    </div>
                </div>

                <!-- Stats -->
                <div class="flex flex-wrap items-center justify-center gap-6 md:gap-10 mt-10">
                    <div class="text-center">
                        <p class="text-3xl font-bold text-rose-500">{blogPosts.length}+</p>
                        <p class="text-sm text-gray-600">Articles</p>
                    </div>
                    <div class="text-center">
                        <p class="text-3xl font-bold text-rose-500">4</p>
                        <p class="text-sm text-gray-600">Thématiques</p>
                    </div>
                    <div class="text-center">
                        <p class="text-3xl font-bold text-rose-500">2/sem</p>
                        <p class="text-sm text-gray-600">Nouveautés</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Categories Filter - AMÉLIORÉE -->
        <section class="sticky top-20 z-30 py-6 bg-white/95 backdrop-blur-md border-b-2 border-rose-100 shadow-sm">
            <div class="container mx-auto px-4 lg:px-8">
                <div class="flex flex-wrap justify-center items-center gap-3">
                    <span class="text-sm font-semibold text-gray-600 mr-2">Filtrer :</span>
                    
                    <button class="group px-6 py-2.5 bg-gradient-to-r from-rose-400 to-coral-500 text-white rounded-full font-semibold hover:scale-105 transition-all shadow-lg">
                        <span class="flex items-center gap-2">
                            Tous
                            <span class="bg-white/30 px-2 py-0.5 rounded-full text-xs">{categories['Tous']}</span>
                        </span>
                    </button>
                    
                    <button class="group px-6 py-2.5 bg-white border-2 border-rose-200 text-gris-texte rounded-full font-semibold hover:bg-gradient-to-r hover:from-rose-50 hover:to-peche-light/30 hover:border-rose-300 transition-all">
                        <span class="flex items-center gap-2">
                            🧘 Yoga
                            <span class="bg-rose-100 text-rose-600 px-2 py-0.5 rounded-full text-xs font-bold">{categories['Yoga']}</span>
                        </span>
                    </button>
                    
                    <button class="group px-6 py-2.5 bg-white border-2 border-rose-200 text-gris-texte rounded-full font-semibold hover:bg-gradient-to-r hover:from-rose-50 hover:to-peche-light/30 hover:border-rose-300 transition-all">
                        <span class="flex items-center gap-2">
                            💅 Prothésie ongulaire
                            <span class="bg-rose-100 text-rose-600 px-2 py-0.5 rounded-full text-xs font-bold">{categories['Prothésie Ongulaire']}</span>
                        </span>
                    </button>
                    
                    <button class="group px-6 py-2.5 bg-white border-2 border-rose-200 text-gris-texte rounded-full font-semibold hover:bg-gradient-to-r hover:from-rose-50 hover:to-peche-light/30 hover:border-rose-300 transition-all">
                        <span class="flex items-center gap-2">
                            ✨ Esthétique
                            <span class="bg-rose-100 text-rose-600 px-2 py-0.5 rounded-full text-xs font-bold">{categories['Esthétique']}</span>
                        </span>
                    </button>
                    
                    <button class="group px-6 py-2.5 bg-white border-2 border-rose-200 text-gris-texte rounded-full font-semibold hover:bg-gradient-to-r hover:from-rose-50 hover:to-peche-light/30 hover:border-rose-300 transition-all">
                        <span class="flex items-center gap-2">
                            🎉 Événements
                            <span class="bg-rose-100 text-rose-600 px-2 py-0.5 rounded-full text-xs font-bold">{categories['Événements']}</span>
                        </span>
                    </button>
                </div>
            </div>
        </section>

        <!-- Blog Posts Grid - ULTRA AMÉLIORÉE -->
        <section class="py-16 md:py-24 bg-gradient-to-b from-white to-gray-50">
            <div class="container mx-auto px-4 lg:px-8">
                
                <!-- Article en vedette (le plus récent) -->
                {blogPosts.length > 0 && (
                    <div class="mb-16">
                        <div class="flex items-center gap-2 mb-6">
                            <svg class="w-6 h-6 text-rose-500" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                            </svg>
                            <h2 class="text-2xl font-display font-bold text-gray-800">Article en vedette</h2>
                        </div>
                        
                        <article class="group bg-white rounded-3xl overflow-hidden border-2 border-rose-200 hover:border-rose-400 hover:shadow-2xl transition-all duration-300">
                            <div class="grid grid-cols-1 lg:grid-cols-2 gap-0">
                                <!-- Image -->
                                <div class={`relative h-64 lg:h-auto w-full overflow-hidden flex items-center justify-center ${
                                    blogPosts[0].slug.length % 3 === 0 ? 'bg-gradient-to-br from-rose-100 to-rose-200' : 
                                    blogPosts[0].slug.length % 3 === 1 ? 'bg-gradient-to-br from-peche-light to-peche-clair' : 
                                    'bg-gradient-to-br from-purple-100 to-pink-100'
                                }`}>
                                    <span class="text-8xl">
                                        {blogPosts[0].data.category === 'Yoga' ? '🧘‍♀️' :
                                         blogPosts[0].data.category === 'Prothésie Ongulaire' ? '💅' :
                                         blogPosts[0].data.category === 'Esthétique' ? '✨' : '🎉'}
                                    </span>
                                    <div class="absolute top-4 left-4">
                                        <span class="bg-rose-500 text-white px-4 py-2 rounded-full text-sm font-bold shadow-lg">
                                            ⭐ Nouveau
                                        </span>
                                    </div>
                                </div>

                                <!-- Contenu -->
                                <div class="p-8 md:p-10 flex flex-col justify-center">
                                    <div class="flex items-center gap-3 mb-4">
                                        <span class="px-4 py-1.5 bg-gradient-to-r from-rose-100 to-peche-light/50 text-rose-600 rounded-full text-sm font-bold">
                                            {blogPosts[0].data.category}
                                        </span>
                                        <span class="text-sm text-gray-500 flex items-center gap-1">
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                                            </svg>
                                            {blogPosts[0].data.pubDate.toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' })}
                                        </span>
                                    </div>
                                    
                                    <h3 class="text-3xl md:text-4xl font-display font-bold text-gris-texte mb-4 group-hover:text-rose-500 transition-colors">
                                        <a href={`/blog/${blogPosts[0].slug}`} class="before:absolute before:inset-0">
                                            {blogPosts[0].data.title}
                                        </a>
                                    </h3>
                                    
                                    <p class="text-lg text-gray-600 leading-relaxed mb-6">
                                        {blogPosts[0].data.description}
                                    </p>
                                    
                                    <div class="flex items-center gap-3">
                                        <a 
                                            href={`/blog/${blogPosts[0].slug}`}
                                            class="inline-flex items-center gap-2 bg-gradient-to-r from-rose-400 to-coral-500 text-white px-8 py-3 rounded-full font-bold hover:scale-105 hover:shadow-xl transition-all"
                                        >
                                            Lire l'article
                                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
                                            </svg>
                                        </a>
                                        <span class="text-sm text-gray-500">⏱️ 5 min de lecture</span>
                                    </div>
                                </div>
                            </div>
                        </article>
                    </div>
                )}

                <!-- Titre section -->
                <div class="flex items-center justify-between mb-10">
                    <h2 class="text-3xl font-display font-bold text-gray-800">Tous nos articles</h2>
                    <div class="flex items-center gap-2 text-gray-600">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4h13M3 8h9m-9 4h9m5-4v12m0 0l-4-4m4 4l4-4"/>
                        </svg>
                        <span class="text-sm font-medium">Trier par : Plus récents</span>
                    </div>
                </div>

                <!-- Grid articles -->
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {blogPosts.slice(1).map((post) => (
                        <article class="group bg-white rounded-2xl overflow-hidden border-2 border-rose-100 hover:border-rose-300 hover:-translate-y-2 hover:shadow-2xl transition-all duration-300 flex flex-col h-full">
                            <!-- Image -->
                            <div class={`relative h-56 w-full overflow-hidden flex items-center justify-center ${
                                post.slug.length % 3 === 0 ? 'bg-gradient-to-br from-rose-100 to-rose-200' : 
                                post.slug.length % 3 === 1 ? 'bg-gradient-to-br from-peche-light to-peche-clair' : 
                                'bg-gradient-to-br from-purple-100 to-pink-100'
                            }`}>
                                <span class="text-6xl group-hover:scale-110 transition-transform duration-300">
                                    {post.data.category === 'Yoga' ? '🧘‍♀️' :
                                     post.data.category === 'Prothésie Ongulaire' ? '💅' :
                                     post.data.category === 'Esthétique' ? '✨' : '🎉'}
                                </span>
                                
                                <!-- Badge catégorie overlay -->
                                <div class="absolute top-4 left-4">
                                    <span class="bg-white/95 backdrop-blur-sm text-gray-800 px-3 py-1 rounded-full text-xs font-bold shadow-md">
                                        {post.data.category}
                                    </span>
                                </div>
                            </div>

                            <!-- Content -->
                            <div class="p-6 flex flex-col flex-grow">
                                <div class="flex items-center gap-2 mb-3 text-sm text-gray-500">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                                    </svg>
                                    {post.data.pubDate.toLocaleDateString('fr-FR', { day: 'numeric', month: 'short', year: 'numeric' })}
                                    <span class="mx-2">•</span>
                                    <span>⏱️ 5 min</span>
                                </div>
                                
                                <h3 class="text-xl font-display font-bold text-gris-texte mb-3 group-hover:text-rose-500 transition-colors line-clamp-2">
                                    <a href={`/blog/${post.slug}`} class="before:absolute before:inset-0">
                                        {post.data.title}
                                    </a>
                                </h3>
                                
                                <p class="text-gray-600 leading-relaxed mb-4 line-clamp-3 flex-grow">
                                    {post.data.description}
                                </p>
                                
                                <div class="flex items-center justify-between mt-auto pt-4 border-t border-gray-100">
                                    <div class="text-rose-500 font-semibold group-hover:gap-3 transition-all duration-300 flex items-center gap-2">
                                        <span>Lire la suite</span>
                                        <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                                        </svg>
                                    </div>
                                    
                                    <!-- Bouton partage -->
                                    <button class="p-2 hover:bg-rose-50 rounded-full transition-colors" aria-label="Partager">
                                        <svg class="w-5 h-5 text-gray-400 hover:text-rose-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"/>
                                        </svg>
                                    </button>
                                </div>
                            </div>
                        </article>
                    ))}
                </div>

                <!-- Pagination (si beaucoup d'articles) -->
                {blogPosts.length > 12 && (
                    <div class="flex justify-center items-center gap-2 mt-16">
                        <button class="px-4 py-2 border-2 border-rose-200 rounded-lg text-gray-600 hover:bg-rose-50 transition-colors">
                            ← Précédent
                        </button>
                        <button class="px-4 py-2 bg-gradient-to-r from-rose-400 to-coral-500 text-white rounded-lg font-semibold">
                            1
                        </button>
                        <button class="px-4 py-2 border-2 border-rose-200 rounded-lg text-gray-600 hover:bg-rose-50 transition-colors">
                            2
                        </button>
                        <button class="px-4 py-2 border-2 border-rose-200 rounded-lg text-gray-600 hover:bg-rose-50 transition-colors">
                            3
                        </button>
                        <button class="px-4 py-2 border-2 border-rose-200 rounded-lg text-gray-600 hover:bg-rose-50 transition-colors">
                            Suivant →
                        </button>
                    </div>
                )}
            </div>
        </section>

        <!-- Newsletter Section -->
        <section class="py-16 bg-gradient-to-br from-rose-50 via-peche-light/20 to-white">
            <div class="container mx-auto px-4 lg:px-8">
                <div class="max-w-3xl mx-auto bg-white rounded-3xl p-8 md:p-12 border-2 border-rose-200 shadow-xl text-center">
                    <div class="w-16 h-16 bg-gradient-to-r from-rose-400 to-coral-500 rounded-full flex items-center justify-center mx-auto mb-6">
                        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                        </svg>
                    </div>
                    
                    <h3 class="text-3xl font-display font-bold text-gray-800 mb-4">
                        Recevez nos conseils bien-être
                    </h3>
                    <p class="text-lg text-gray-600 mb-8">
                        Inscrivez-vous à notre newsletter et recevez <strong>2 fois par mois</strong> nos meilleurs articles, astuces beauté et offres exclusives !
                    </p>
                    
                    <form class="flex flex-col sm:flex-row gap-3 max-w-xl mx-auto">
                        <input 
                            type="email" 
                            placeholder="Votre email"
                            class="flex-1 px-6 py-4 rounded-full border-2 border-rose-200 focus:border-rose-400 focus:outline-none"
                            required
                        />
                        <button 
                            type="submit"
                            class="bg-gradient-to-r from-rose-400 to-coral-500 text-white px-8 py-4 rounded-full font-bold hover:scale-105 hover:shadow-xl transition-all whitespace-nowrap"
                        >
                            S'inscrire ✨
                        </button>
                    </form>
                    
                    <p class="text-xs text-gray-500 mt-4">
                        🔒 Vos données sont protégées · Désabonnement en 1 clic
                    </p>
                </div>
            </div>
        </section>

        <!-- CTA Final -->
        <section class="py-16 bg-white">
            <div class="container mx-auto px-4 text-center">
                <h3 class="text-3xl font-display font-bold text-gray-800 mb-4">
                    Une Question ? Besoin d'un Conseil Personnalisé ?
                </h3>
                <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto">
                    Nos expertes sont à votre écoute pour vous conseiller sur le yoga, la beauté ou l'organisation de votre événement à Lorient.
                </p>
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a
                        href="tel:+33618910216"
                        class="inline-flex items-center gap-3 bg-gradient-to-r from-green-500 to-green-600 text-white px-10 py-5 rounded-full font-bold text-lg hover:scale-105 hover:shadow-2xl transition-all"
                    >
                        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z"/>
                        </svg>
                        Appeler : 06 18 91 02 16
                    </a>
                    <a
                        href="/contact"
                        class="inline-flex items-center gap-2 bg-white border-2 border-rose-300 text-rose-600 px-10 py-5 rounded-full font-bold text-lg hover:bg-rose-50 transition-all"
                    >
                        Formulaire contact
                    </a>
                </div>
            </div>
        </section>
    </main>

    <Footer />
</Layout>

<style>
    /* Animation pour les filtres actifs */
    button:active {
        transform: scale(0.95);
    }
    
    /* Smooth scroll pour navigation */
    html {
        scroll-behavior: smooth;
    }
</style>